module.exports = {
  refactorResponse: (userResponse) => {

  },

  querySetup: (query) => {
    let { lat, lon, category, radius, followers_min, followers_max } = query;
    let q = {

    };

    return q;
  }
};